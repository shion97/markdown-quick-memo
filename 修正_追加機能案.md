# 修正レポート
## <editor>
  - リストのインデントについて、十字キーによる移動をすると綺麗にそろうが、enter,Back spaceを入力すると、2行目以降が一文字右に上がる。

  - 恐らく、十字キーで移動した際はリストの点や数字が少し左側に動く。それによりインデントがちょうどよくなる

  - enter or back spaceを入力した後の状態を常にキープしたい。その状態でインデントを一行目と一致させたい

  -  レベル2以降はインデントは正しく整っている

  - リストのレベルごとのインデントの違いも整えたい(レベルごとのインデント上昇値を一定にする)(tabによるレベル移動一回分が同じ)

  - レベルごとのインデントの差はレベル1->2で  
