## 2026-08-10 `表セルの左右キー移動を修正`
commit : `表セル間の左右キー移動を修正`
branch : `feat/tauri-migration`
### 要件
表内で左右キーを複数回押さなくても、隣接するセルへ移動できるようにする。
### 対応
既存の表セル範囲解析を入力支援から再利用し、カーソルがセル端にある場合だけ非表示の区切り記号と空白を飛ばして隣接セルへ移動するよう変更した。セル内と表以外では従来どおり一文字ずつ移動する。空セルを含む左右移動の回帰テストを追加した。
### 検証結果
- 関連テスト: 31件成功
- `pnpm test`: 86件成功
- `pnpm run lint`: 成功
- `pnpm run build`: 型チェックとViteビルド成功
- `scripts/build.ps1`: JavaScript 86件、Rust 12件が成功し、本体とホットキーランチャーのEXE作成成功
- `scripts/create_shortcut.ps1`: スタートメニューショートカットとログオンタスクの登録成功
- 非UI検証: タスク実行中、優先度4、ホットキー`CTRL+ALT+M`、旧Runキーなし、本体とランチャーが各1プロセス起動
