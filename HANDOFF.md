# 引継ぎメモ

## 2026-07-27 修正・追加機能

- リスト項目の折り返し行を本文先頭へ揃える吊り下げインデントを追加。
- 保存先フォルダをエクスプローラーで開くファイルメニューを追加。
- PDF生成成功後、OSの既定アプリでPDFを自動的に開く処理を追加。
- 開いている保存済みファイルを同一フォルダ内でリネームする機能を追加。既存ファイルは上書きしない。
- GUIテストとMarkdown解析テストを追加・更新。

## 検証状況

- `venv\Scripts\python.exe -m unittest discover -s tests -v`
  - 79件成功、失敗0件、32件スキップ。
  - スキップは実行環境のTcl/Tk不足（`Can't find a usable init.tcl`）によるGUIスモークテスト。
  - Tkを生成しないファイルリネーム、保存先オープン、PDF自動オープンの単体テストは成功。
- `venv\Scripts\python.exe -m compileall -q markdown_quick_memo tests`
  - 成功。
- `git diff --check`
  - 問題なし。

## 2026-07-27 追加修正

- 折り返しインデントを、原文マーカー幅ではなく実際のプレビュー用マーカー幅から算出するよう修正。
- カーソル移動によって原文記号とプレビュー記号が切り替わる際も折り返し位置を再計算。
- 番号付きリストでEnterを押した際、保存原文へ常に`1. `を追加するよう変更。
- `docs/code-map/`へ編集入力支援、ファイル管理、PDF出力の索引を追加。
- 近接する非GUIテスト13件、`compileall`、`git diff --check`は成功。
- 折り返しとEnter入力のGUIテスト2件は、実行環境のTcl/Tk不足によりスキップ。

## 2026-07-27 入力直後の折り返し修正

- アクティブなリスト行の原文マーカーを、実際の太字フォント幅で計算するよう修正。
- ファイル名変更へ`Ctrl+Shift+R`、保存先フォルダ表示へ`Ctrl+Shift+E`を割り当て。
- README、設計書、コードマップ、GUIテストを更新。
- 近接する非GUIテスト19件、`compileall`、`git diff --check`は成功。GUIテスト2件はTcl/Tk不足によりスキップ。

## 2026-07-27 リスト配置の再修正

- 原文・プレビューの最大幅から共通マーカー列を作り、双方を右揃えする方式へ変更。
- アクティブ行は原文マーカー幅の不足分を左余白で補い、非アクティブ行は共通幅のプレビュー領域を使ってカーソル移動時の位置変化を防止。
- 階層間の本文位置差をTab一回分の半角スペース2個へ統一。
- 近接する非GUIテスト34件、リスト配置のGUIテスト2件、`compileall`、`git diff --check`は成功。

## 2026-07-29 `README`・設計書の機能記載を同期
comit : `README`・設計書の機能記載を同期
branch : `docs/sync-readme-design`
### 要件
`Ctrl + Shift + E` の保存先フォルダ表示機能をREADMEへ明記し、実装済み機能とREADME・設計書の齟齬を修正する。
### 対応
READMEへ保存先フォルダ表示とファイル名変更の前提条件・動作を追記。設計書へ両機能のショートカット、保存状態、拡張子補完、上書き防止、未保存時の動作を反映。
### 検証結果
実装、テスト、README、設計書を照合し、文書の記載を実装へ合わせた。

## 2026-07-27 プレビュー維持と実座標の統一

- プレビュー変換を維持し、編集行だけ原文マーカーを表示する仕様へ確定。
- プレビューの埋め込み文字へリスト余白タグが付かず、一行目の左余白だけ無視されていたことを実座標テストで特定。
- 原文表示と計測のフォントを統一し、プレビューを余白のない固定幅Canvasへ変更。
- 埋め込みCanvasへリスト余白タグを明示適用し、記号右端、本文先頭、折り返し開始位置を状態間で統一。
- Enter・Backspace・カーソル移動を含む実座標テストを追加し、全83テスト、`compileall`、`git diff --check`は成功。

## 2026-07-29 `code-map` 廃止
comit : `code-map` 廃止
branch : `local/integration-current`
### 要件
廃止した `code-map` を安全に削除する。
### 対応
`docs/code-map/` 配下の索引と機能別ドキュメント4ファイルを削除。
### 検証結果
`docs/code-map/` の削除対象と参照を確認し、アプリ本体・設計資料に現行参照がないことを確認。
